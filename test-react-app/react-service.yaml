apiVersion: v1
kind: Service
metadata:
  name: react-app-service
  namespace: test
spec:
  selector:
    app.kubernetes.io/name: test-react-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
