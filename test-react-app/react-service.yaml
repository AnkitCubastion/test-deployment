apiVersion: v1
kind: Service
metadata:
  name: test-react-app
  namespace: test
spec:
  selector:
    app.kubernetes.io/name: test-react-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
